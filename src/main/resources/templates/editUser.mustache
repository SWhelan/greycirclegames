{{> header}}
{{#user}}
<form class="form-horizontal" action="{{EDIT_USER_ROUTE}}" method="POST">
	<div class="form-group">
    <label for="username" class="col-sm-2 control-label">Username*</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" name="username" id="username" placeholder="User Name" value="{{username}}">
    </div>
  </div>
  <div class="form-group">
    <label for="email" class="col-sm-2 control-label">Email</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" name="email" id="email" placeholder="Email" value="{{email}}">
      <p>We're not going to validate the email only send emails for things like new friends, new games, and when it is your turn to play. You can control when you receive emails below. In fact you don't have to put in an email at all.</p>  
    </div>
  </div>
  <div class="form-group">
  	<label class="col-sm-2 control-label">Send me emails</label>
    <div class="col-sm-10 editUserCheckboxes" id="emailOptions">
      <label class="checkbox"><input type="checkbox" name="emailForNewFriend" id="emailForNewFriend" {{#emailForNewFriend}}checked{{/emailForNewFriend}} value="">When someone adds me as a friend</label>
      <label class="checkbox"><input type="checkbox" name="emailForNewGame" id="emailForNewGame" {{#emailForNewGame}}checked{{/emailForNewGame}} value="">When someone creates a game with me</label>
      <label class="checkbox"><input type="checkbox" name="emailForTurn" id="emailForTurn" {{#emailForTurn}}checked{{/emailForTurn}} value="">When it is my turn in an ongoing game that includes other humans (doesn't send if you are playing against only computer players)</label>
      <label class="checkbox"><input type="checkbox" name="emailForGameOver" id="emailForGameOver" {{#emailForGameOver}}checked{{/emailForGameOver}} value="">When an ongoing game has ended</label>
      <label class="checkbox"><input type="checkbox" name="emailForPoke" id="emailForPoke" {{#emailForPoke}}checked{{/emailForPoke}} value="">When a user pokes me to remind me it is my turn (must be checked if you want to send email reminders to others)</label>
    </div>
  </div>
  <div class="form-group">
    <label class="col-sm-2 control-label">Application Settings</label>
    <div class="col-sm-10 editUserCheckboxes">
      <label class="checkbox"><input type="checkbox" name="showHelpers" id="showHelpers" {{#showHelpers}}checked{{/showHelpers}} value="">Display helpers when playing games</label>
    </div>
    <br><br>
    <div class="col-sm-10 col-sm-offset-2">
        <label for="refreshRate">How often the game page refreshes in seconds. (20 is the default. 0 means never refresh.)</label>
        <input type="text" class="form-control bfh-number" name="refreshRate" value="{{refreshRate}}" data-min="0" data-max="10000" data-wrap="true">
    </div>
  </div>
  <h5>Change Your Password</h5>
  <h6>Only fill out this section if you'd like to change your password.</h6>
  <div class="form-group">
    <label for="currentPassword" class="col-sm-2 control-label">Current Password</label>
    <div class="col-sm-10">
      <input type="password" class="form-control" name="currentPassword" id="currentPassword" placeholder="Current Password">
    </div>
  </div>
  <div class="form-group">
    <label for="newPassword" class="col-sm-2 control-label">New Password</label>
    <div class="col-sm-10">
      <input type="password" class="form-control" name="newPassword" id="newPassword" placeholder="Your shiny new password.">
    </div>
  </div>
  <div class="form-group">
    <label for="newPasswordAgain" class="col-sm-2 control-label">New Password Again</label>
    <div class="col-sm-10">
      <input type="password" class="form-control" name="newPasswordAgain" id="newPasswordAgain" placeholder="Your shiny new password again.">
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-default">Save Changes</button>
    </div>
  </div>
</form>
{{/user}}
{{> footer}}